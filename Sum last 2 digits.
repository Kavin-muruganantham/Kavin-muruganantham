#include <stdio.h>
int main() {
    int num1, num2, a, b, sum;    scanf("%d%d",&num1,&num2);
    a = num1 % 10;
    b = num2 % 10;
    sum = a + b;
    printf("Sum of last digit of num1 and num2 is: %d\n", sum);
    return 0;
}
